# n <- 10

# alpha <- 0.4
# (alphas <- alpha*((1-alpha)^(n:0)))
# embedc(alphas, p) # C # on scaled data

# (y <- ts(rnorm(n+1)))
# p <- 2
# embedc(y, p) # C

# on lags.X
# line 190


# levels <- c(seq(from=50, to=95, by = 5),
#             96, 97, 98, 99)
# -sapply(levels, function (x) qnorm(1-(1-(100-x)/200)))
